{
  "name": "taskflow",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build && cp -r dist ../server/public",
    "start": "cd server && node server.js"
  }
}
